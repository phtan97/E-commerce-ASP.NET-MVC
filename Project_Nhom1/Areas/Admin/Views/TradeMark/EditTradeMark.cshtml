@model Project_Nhom1.Areas.Admin.Models.TradeMarkModel
@{
    ViewBag.Title = "EditTradeMark";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}



<div class="panel panel-primary">
    <div class="panel panel-heading">
        <h4>Chỉnh sửa thương hiệu sản phẩm</h4>
    </div>
    <div class="panel-body">
        @using (Html.BeginForm("EditTradeMark", "TradeMark", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.HiddenFor(x => x.IDTradeMark)
            <div class="row">
                <div class="col-md-6 col-md-offset-1">
                    <div class="form-group has-success">
                        <label class="required">Tên thương hiệu Sản phẩm</label>
                        @Html.TextBoxFor(x => x.Name, new { @class = "form-control" })
                        @Html.ValidationMessageFor(x => x.Name, "", new { @class = "error-message" })
                    </div>
                    <div class="form-group has-success">
                        <label class="control-label" for="inputSuccess">Danh mục sản phẩm</label>
                        @Html.DropDownListFor(x => x.IDCategory, Model.Categories, new { @class = "form-control" })
                        @Html.ValidationMessageFor(x => x.IDCategory, "", new { @class = "error-message" })
                    </div>

                    <div class="form-group has-success">
                        <input type="submit" value="Lưu" class="btn btn-primary">
                    </div>
                    <button onclick="goBack()">Go Back</button>
                </div>
            </div>
        }
    </div>
</div>
<script>
function goBack() {
    window.history.back();
}
</script>


